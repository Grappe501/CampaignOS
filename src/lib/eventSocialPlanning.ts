/**
 * Social planning — thin alias for `eventCommsPlaybooks` (single source of truth for seeds).
 * Prefer importing from `eventCommsPlaybooks` in new code.
 */
export { seedSocialSlots, getCommsPlaybookSeed } from './eventCommsPlaybooks'
